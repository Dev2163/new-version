<div class="custom-hero-container-{{ section.id }}">
  <!-- HERO PART -->
  <div class="custom-hero-{{ section.id }}">
    
    <!-- Main Content Row (Flex reversed if image is left) -->
    <div class="custom-hero-{{ section.id }}__content-row {% if section.settings.image_position == 'left' %}image-left{% endif %}">
      
      <div class="custom-hero-{{ section.id }}__left-wrapper">
        <!-- Far Left Sidebar -->
        <div class="one9-vertical-brand">
          <div class="letter-container" style="display: flex; flex-direction: column; align-items: center;">
            {% assign brand_letters = section.settings.vertical_text | split: "," %}
            {% for letter in brand_letters %}
              <span class="letter">{{ letter }}</span>
            {% endfor %}
          </div>
          <div class="zodiac-icon">
            <svg width="28" height="28" viewBox="0 0 44 44" fill="none" xmlns="http://www.w3.org/2000/svg">
              <!-- Top dots -->
              <circle cx="22" cy="7" r="1.5" fill="#F4C430" />
              <circle cx="15" cy="8.5" r="1.5" fill="#F4C430" />
              <circle cx="29" cy="8.5" r="1.5" fill="#F4C430" />
              <circle cx="9" cy="13" r="1.5" fill="#F4C430" />
              <circle cx="35" cy="13" r="1.5" fill="#F4C430" />
              
              <!-- Blinking Eye Group -->
              <g class="eye-blink-anim">
                <path d="M6 22 Q22 6 38 22 Q22 38 6 22 Z" stroke="#F4C430" stroke-width="2.5" fill="none" />
                <circle cx="22" cy="22" r="4.5" stroke="#F4C430" stroke-width="2.5" fill="none" />
                <circle cx="22" cy="22" r="2" fill="#F4C430" />
              </g>

              <!-- Bottom dots -->
              <circle cx="22" cy="37" r="1.5" fill="#F4C430" />
              <circle cx="15" cy="35.5" r="1.5" fill="#F4C430" />
              <circle cx="29" cy="35.5" r="1.5" fill="#F4C430" />
              <circle cx="9" cy="31" r="1.5" fill="#F4C430" />
              <circle cx="35" cy="31" r="1.5" fill="#F4C430" />
            </svg>
          </div>
          <div class="gold-line"></div>
        </div>
        
        <!-- Text Column -->
        <div class="custom-hero-{{ section.id }}__main">
          <h1 class="custom-hero-{{ section.id }}__title">{{ section.settings.title }}</h1>
          <p class="custom-hero-{{ section.id }}__subtitle">{{ section.settings.subtitle }}</p>
          <a href="{{ section.settings.button_link }}" class="custom-hero-{{ section.id }}__button">{{ section.settings.button_text }}</a>
        </div>
      </div>
      
      <!-- Image Column -->
      <div class="custom-hero-{{ section.id }}__image-col">
        <div class="custom-hero-{{ section.id }}__image-glow">
          {%- if section.settings.image != blank -%}
            {% assign hero_img_class = 'custom-hero-' | append: section.id | append: '__image custom-hero-slideshow-image' %}
            {{ section.settings.image | image_url: width: 1200 | image_tag: class: hero_img_class, id: 'heroMainImage' }}
          {%- else -%}
            {% assign hero_img_class_placeholder = 'custom-hero-' | append: section.id | append: '__image custom-hero-slideshow-image custom-hero-' | append: section.id | append: '__image--placeholder' %}
            {{ 'lifestyle-1' | placeholder_svg_tag: hero_img_class_placeholder }}
          {%- endif -%}
        </div>
      </div>
      
    </div>
  </div>

  <!-- MARQUEE PART -->
  {% if section.settings.show_marquee %}
  <div class="custom-marquee-{{ section.id }}">
    <div class="custom-marquee-{{ section.id }}__track">
      <div class="custom-marquee-{{ section.id }}__content">
        {%- for i in (1..10) -%}
          <span class="custom-marquee-{{ section.id }}__text">{{ section.settings.marquee_text }}</span>
          <span class="custom-marquee-{{ section.id }}__icon">✦</span>
        {%- endfor -%}
      </div>
      <div class="custom-marquee-{{ section.id }}__content">
        {%- for i in (1..10) -%}
          <span class="custom-marquee-{{ section.id }}__text">{{ section.settings.marquee_text }}</span>
          <span class="custom-marquee-{{ section.id }}__icon">✦</span>
        {%- endfor -%}
      </div>
    </div>
  </div>
  {% endif %}

  <!-- THUMBNAIL STRIP PART -->
  {% if section.blocks.size > 0 %}
  <div class="thumbnail-strip-{{ section.id }}">
    <div class="thumbnail-strip-{{ section.id }}__container">
      {% for block in section.blocks %}
        {% if block.type == 'thumbnail' %}
        <a href="{{ block.settings.product_url | default: '#' }}" class="thumbnail-strip-{{ section.id }}__card" {{ block.shopify_attributes }}>
          <div class="thumbnail-strip-{{ section.id }}__image-wrapper">
            {% if block.settings.image != blank %}
              {% assign thumb_img_class = 'thumbnail-strip-' | append: section.id | append: '__image' %}
              {{ block.settings.image | image_url: width: 400 | image_tag: class: thumb_img_class }}
            {% else %}
              {% assign thumb_placeholder_class = 'thumbnail-strip-' | append: section.id | append: '__image thumbnail-strip-' | append: section.id | append: '__placeholder' %}
              {{ 'product-1' | placeholder_svg_tag: thumb_placeholder_class }}
            {% endif %}
            <div class="thumbnail-strip-{{ section.id }}__hover-name">
              {{ block.settings.product_name | default: 'Libra Balance Oversized Tee' }}
            </div>
          </div>
          <div class="thumbnail-strip-{{ section.id }}__info">
            <div class="thumbnail-strip-{{ section.id }}__color-wrap">
              <span class="thumbnail-strip-{{ section.id }}__dot" style="background-color: {{ block.settings.color_hex }};"></span>
              <span class="thumbnail-strip-{{ section.id }}__text">{{ block.settings.color_name }}</span>
            </div>
            <span class="thumbnail-strip-{{ section.id }}__price">{{ block.settings.price }}</span>
          </div>
        </a>
        {% endif %}
      {% endfor %}
    </div>
  </div>
  {% endif %}
</div>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@800;900&family=Rajdhani:wght@600;700&display=swap');

  .custom-hero-container-{{ section.id }} {
    width: 100%;
    background-color: {{ section.settings.bg_color }};
    overflow: hidden;
  }

  /* --- HERO CSS --- */
  .custom-hero-{{ section.id }} {
    position: relative;
    width: 100%;
    min-height: 700px;
    display: flex;
    color: {{ section.settings.text_color }};
  }

  .custom-hero-{{ section.id }}__left-wrapper {
    flex: 1.2;
    display: flex;
    flex-direction: row;
    align-items: center; /* Center vertically relative to each other */
    padding-left: 28px; /* Left padding for the whole block */
    max-width: 900px;
  }

  .one9-vertical-brand {
    display: flex;
    flex-direction: column;
    align-items: center;
    color: #F4C430;
    margin-right: 48px; /* Space between sidebar and text */
    flex-shrink: 0;
  }

  .one9-vertical-brand .letter {
    font-family: "Orbitron", "Rajdhani", sans-serif;
    font-size: 36px;
    font-weight: 900;
    line-height: 0.95;
    letter-spacing: 1px;
    color: #F4C430;
    text-shadow: 0 0 15px rgba(244, 196, 48, 0.25);
    display: block;
    text-transform: uppercase;
  }

  .one9-vertical-brand .zodiac-icon {
    width: 28px;
    height: 28px;
    margin-top: 24px;
    margin-bottom: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .one9-vertical-brand .gold-line {
    width: 2px;
    height: 160px; /* Slightly longer to match image */
    background: #F4C430;
    box-shadow: 0 0 8px rgba(244, 196, 48, 0.22);
  }

  .custom-hero-{{ section.id }}__content-row {
    flex: 1;
    display: flex;
    flex-direction: row;
    align-items: stretch;
  }
  .custom-hero-{{ section.id }}__content-row.image-left {
    flex-direction: row-reverse;
  }
  
  .eye-blink-anim {
    transform-origin: 22px 22px;
    animation: eyeBlink 4s infinite;
  }
  
  @keyframes eyeBlink {
    0%, 93%, 100% { transform: scaleY(1); }
    96.5% { transform: scaleY(0.1); }
  }
  
  .custom-hero-{{ section.id }}__main {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 40px 40px 40px 0; /* Left padding removed as wrapper handles it */
  }
  
  .custom-hero-{{ section.id }}__image-col {
    flex: 1;
    display: flex;
    justify-content: flex-end;
    align-items: stretch;
    padding: 0;
    position: relative;
    overflow: hidden;
  }
  
  .custom-hero-{{ section.id }}__image-glow {
    position: relative;
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: flex-end;
    background: radial-gradient(circle at 50% 50%, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.08) 40%, transparent 75%);
  }
  
  .custom-hero-{{ section.id }}__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: right center;
    z-index: 2;
    position: relative;
    transition: opacity 0.5s ease;
    transform: scale(0.85);
    transform-origin: right bottom;
  }
  
  .custom-hero-{{ section.id }}__title {
    font-size: {{ section.settings.heading_size }}px;
    font-weight: 900;
    line-height: 1.05;
    text-transform: uppercase;
    margin: 0 0 24px 0;
    color: {{ section.settings.heading_color }};
    font-family: "Orbitron", "Rajdhani", sans-serif;
    letter-spacing: 1px;
    white-space: pre-line;
    text-shadow: 0 0 15px {{ section.settings.heading_color | color_modify: 'alpha', 0.25 }};
  }
  .custom-hero-{{ section.id }}__subtitle {
    font-size: {{ section.settings.subheading_size }}px;
    color: {{ section.settings.subheading_color }};
    margin: 0 0 48px 0;
    font-family: 'Inter', 'Helvetica Neue', sans-serif;
    font-weight: 400;
  }
  .custom-hero-{{ section.id }}__button {
    display: inline-block;
    background: {{ section.settings.accent_color }};
    color: #000000;
    text-transform: uppercase;
    font-weight: 800;
    font-size: 1.125rem;
    padding: 16px 52px;
    text-decoration: none;
    border-radius: 4px;
    box-shadow: 0 0 30px {{ section.settings.accent_color | color_modify: 'alpha', 0.4 }};
    align-self: flex-start;
    transition: all 0.3s ease;
    letter-spacing: 1px;
  }
  .custom-hero-{{ section.id }}__button:hover {
    box-shadow: 0 0 40px {{ section.settings.accent_color | color_modify: 'alpha', 0.6 }};
    transform: translateY(-2px);
    filter: brightness(1.1);
  }

  /* --- MARQUEE CSS --- */
  .custom-marquee-{{ section.id }} {
    width: 100%;
    background-color: {{ section.settings.marquee_bg_color }};
    overflow: hidden;
    padding: 12px 0;
    position: relative;
    border-top: 1px solid rgba(255,255,255,0.05);
    border-bottom: 1px solid rgba(255,255,255,0.05);
  }
  .custom-marquee-{{ section.id }}__track {
    display: flex;
    width: max-content;
    animation: marquee-scroll-{{ section.id }} {{ section.settings.marquee_speed }}s linear infinite;
  }
  .custom-marquee-{{ section.id }}__content {
    display: flex;
    align-items: center;
    justify-content: space-around;
  }
  .custom-marquee-{{ section.id }}__text {
    color: {{ section.settings.marquee_text_color }};
    font-family: 'Inter', 'Helvetica Neue', sans-serif;
    font-size: 13px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 2px;
    white-space: nowrap;
    margin: 0 24px;
  }
  .custom-marquee-{{ section.id }}__icon {
    color: {{ section.settings.marquee_text_color }};
    font-size: 12px;
  }
  @keyframes marquee-scroll-{{ section.id }} {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  /* --- THUMBNAIL STRIP CSS --- */
  .thumbnail-strip-{{ section.id }} {
    background-color: transparent;
    padding: 20px 40px;
    width: 100%;
    box-sizing: border-box;
  }
  .thumbnail-strip-{{ section.id }}__container {
    display: flex;
    justify-content: center;
    gap: 24px;
    max-width: 1200px;
    margin: 0 auto;
  }
  .thumbnail-strip-{{ section.id }}__card {
    flex: 1 1 25%;
    max-width: 280px;
    background-color: transparent;
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 10px;
    overflow: hidden;
    position: relative;
    cursor: pointer;
    transition: all 0.3s ease;
    text-decoration: none;
    display: flex;
    flex-direction: column;
  }
  .thumbnail-strip-{{ section.id }}__card:hover {
    transform: scale(1.03);
    border-color: {{ section.settings.accent_color }};
    box-shadow: 0 4px 20px {{ section.settings.accent_color | color_modify: 'alpha', 0.15 }};
  }
  .thumbnail-strip-{{ section.id }}__image-wrapper {
    width: 100%;
    aspect-ratio: 1 / 1.1;
    overflow: hidden;
    position: relative;
  }
  .thumbnail-strip-{{ section.id }}__hover-name {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0, 0, 0, 0.7);
    color: #FFFFFF;
    padding: 20px;
    font-size: 16px;
    font-weight: 700;
    text-align: center;
    opacity: 0;
    transition: opacity 0.3s ease;
  }
  .thumbnail-strip-{{ section.id }}__card:hover .thumbnail-strip-{{ section.id }}__hover-name {
    opacity: 1;
  }
  .thumbnail-strip-{{ section.id }}__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .thumbnail-strip-{{ section.id }}__placeholder {
    fill: #333;
    background: {{ section.settings.card_bg_color }};
  }
  .thumbnail-strip-{{ section.id }}__info {
    width: 100%;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    padding: 16px;
    background-color: {{ section.settings.card_bg_color }};
  }
  .thumbnail-strip-{{ section.id }}__color-wrap {
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .thumbnail-strip-{{ section.id }}__dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    border: 1px solid rgba(255,255,255,0.2);
    display: block;
  }
  .thumbnail-strip-{{ section.id }}__text {
    color: {{ section.settings.text_color }};
    font-family: 'Inter', 'Helvetica Neue', sans-serif;
    font-size: 14px;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 1px;
  }
  .thumbnail-strip-{{ section.id }}__price {
    color: {{ section.settings.text_color | color_modify: 'alpha', 0.8 }};
    font-family: 'Inter', 'Helvetica Neue', sans-serif;
    font-size: 13px;
    font-weight: 400;
  }

  /* --- RESPONSIVE CSS --- */
  @media (max-width: 1200px) {
    .custom-hero-{{ section.id }}__title { font-size: 4rem; }
  }
  @media (max-width: 990px) {
    .custom-hero-{{ section.id }}__title { font-size: 3.5rem; }
    .custom-hero-{{ section.id }}__subtitle { font-size: 1.15rem; }
    .custom-hero-{{ section.id }}__button { padding: 14px 40px; font-size: 1rem; }
  }
  @media (max-width: 768px) {
    /* Header overrides on mobile Index for ONE9 premium look */
    #shopify-section-header,
    #shopify-section-header .header-wrapper,
    #shopify-section-header sticky-header,
    #shopify-section-header .header__row {
      background-color: #0A0A0A !important;
    }
    
    #header-component {
      background-color: #0A0A0A !important;
      border-bottom: 1px solid rgba(255,255,255,0.05) !important;
    }
    
    /* Make icons/hamburger white */
    #header-component svg path,
    #header-component svg circle,
    #header-component svg line {
      stroke: #FFFFFF !important;
    }
    
    /* Cart count badge to match theme */
    .cart-count-bubble {
      background-color: #F4C430 !important;
      color: #000000 !important;
      font-weight: 800 !important;
    }

    /* Hero Layout: Mobile-first 390x844 responsive */
    .custom-hero-{{ section.id }} {
      display: block !important;
      position: relative !important;
      width: 100% !important;
      height: 560px !important;
      min-height: 560px !important;
      background-color: #0A0A0A !important;
      overflow: hidden !important;
    }

    /* Keep vertical ONE9 branding on far-left but smaller, not overlapping, cleared from header */
    .one9-vertical-brand {
      position: absolute !important;
      left: 16px !important;
      top: 76px !important; /* Pushed down below fixed header to prevent cutoff */
      z-index: 5 !important;
      display: flex !important;
      flex-direction: column !important;
      align-items: center !important;
      width: 40px !important;
      padding: 0 !important;
    }
    
    .one9-vertical-brand .letter-container {
      margin-bottom: 8px !important;
    }
    
    .one9-vertical-brand .letter {
      font-size: 20px !important; /* Smaller on mobile */
      line-height: 0.95 !important;
      text-shadow: 0 0 8px rgba(244, 196, 48, 0.18) !important;
    }
    
    .one9-vertical-brand .zodiac-icon {
      width: 20px !important;
      height: 20px !important;
      margin-top: 12px !important;
      margin-bottom: 8px !important;
    }
    
    .one9-vertical-brand .zodiac-icon svg {
      width: 20px !important;
      height: 20px !important;
    }
    
    .one9-vertical-brand .gold-line {
      width: 1px !important;
      height: 90px !important;
      background-color: #F4C430 !important;
      box-shadow: 0 0 6px rgba(244, 196, 48, 0.22) !important;
    }

    /* Model image occupies upper-right portion with safe crops */
    .custom-hero-{{ section.id }}__content-row {
      display: block !important;
      position: relative !important;
      width: 100% !important;
      height: 100% !important;
    }
    
    .custom-hero-{{ section.id }}__image-col {
      position: absolute !important;
      top: 60px !important; /* Pushed down below fixed header to prevent cutting off model's head */
      right: 0 !important;
      width: 72% !important; /* Slightly wider to show model details */
      height: 65% !important; /* Taller to prevent cutting off model's head */
      z-index: 1 !important;
      padding: 0 !important;
      margin: 0 !important;
      overflow: hidden !important;
    }
    
    /* Gradients to blend model with solid background */
    .custom-hero-{{ section.id }}__image-col::before {
      content: '' !important;
      position: absolute !important;
      top: 0 !important;
      left: 0 !important;
      bottom: 0 !important;
      width: 50% !important;
      background: linear-gradient(to right, #0A0A0A 0%, transparent 100%) !important;
      z-index: 3 !important;
      pointer-events: none !important;
    }
    
    .custom-hero-{{ section.id }}__image-col::after {
      content: '' !important;
      position: absolute !important;
      bottom: 0 !important;
      left: 0 !important;
      right: 0 !important;
      height: 40% !important;
      background: linear-gradient(to top, #0A0A0A 0%, transparent 100%) !important;
      z-index: 3 !important;
      pointer-events: none !important;
    }

    .custom-hero-{{ section.id }}__image {
      width: 100% !important;
      height: 100% !important;
      object-fit: cover !important;
      object-position: center 15% !important; /* Focuses crop slightly lower to keep head visible */
    }

    /* Headline on the left/lower section, safe spacing from sidebar */
    .custom-hero-{{ section.id }}__main {
      position: absolute !important;
      bottom: 0 !important;
      left: 0 !important;
      width: 100% !important;
      z-index: 4 !important;
      padding: 0px 20px 48px 60px !important; /* Left padding 60px avoids sidebar and gives title more space */
      box-sizing: border-box !important;
      display: flex !important;
      flex-direction: column !important;
      align-items: flex-start !important;
      text-align: left !important;
    }

    .custom-hero-{{ section.id }}__title {
      font-size: 20px !important; /* Reduced to 20px so it fits on exactly 2 rows on mobile */
      line-height: 1.15 !important;
      margin-bottom: 12px !important;
      text-transform: uppercase !important;
      font-family: 'Archivo Black', 'Anton', sans-serif !important;
      color: #FFFFFF !important;
      letter-spacing: 0.5px !important;
      white-space: pre-line !important;
    }

    .custom-hero-{{ section.id }}__subtitle {
      font-size: 12px !important;
      line-height: 1.4 !important;
      margin-bottom: 24px !important;
      color: #B0B0B0 !important;
      max-width: 250px !important;
      text-align: left !important;
    }

    /* Gold CTA button with gold glow */
    .custom-hero-{{ section.id }}__button {
      align-self: flex-start !important;
      padding: 12px 36px !important;
      font-size: 12px !important;
      font-weight: 800 !important;
      background-color: #F4C430 !important;
      color: #000000 !important;
      border-radius: 4px !important;
      box-shadow: 0 0 25px rgba(244, 196, 48, 0.5) !important;
      letter-spacing: 1px !important;
      text-transform: uppercase !important;
      transition: all 0.3s ease !important;
    }

    /* Below Hero: Horizontal scrollable jacket variants */
    .thumbnail-strip-{{ section.id }} {
      padding: 24px 20px !important;
      background-color: #0A0A0A !important;
    }
    
    .thumbnail-strip-{{ section.id }}__container {
      justify-content: flex-start !important;
      overflow-x: auto !important;
      scroll-snap-type: x mandatory !important;
      padding-bottom: 8px !important;
      -webkit-overflow-scrolling: touch !important;
      display: flex !important;
      gap: 16px !important;
    }
    
    /* Hide scrollbar completely across all browsers */
    .thumbnail-strip-{{ section.id }}__container {
      -ms-overflow-style: none !important; /* IE/Edge */
      scrollbar-width: none !important; /* Firefox */
    }
    .thumbnail-strip-{{ section.id }}__container::-webkit-scrollbar,
    .thumbnail-strip-{{ section.id }}__container::-webkit-scrollbar-thumb,
    .thumbnail-strip-{{ section.id }}__container::-webkit-scrollbar-track,
    .thumbnail-strip-{{ section.id }}__container::-webkit-scrollbar-button {
      display: none !important;
      width: 0 !important;
      height: 0 !important;
      background: transparent !important;
      opacity: 0 !important;
    }
    
    /* Rounded variant cards */
    .thumbnail-strip-{{ section.id }}__card {
      flex: 0 0 62% !important; /* 62% width so next card is partially visible */
      scroll-snap-align: start !important;
      border-radius: 12px !important;
      border: 1px solid rgba(255, 255, 255, 0.06) !important;
      background-color: #111111 !important;
      overflow: hidden !important;
      display: flex !important;
      flex-direction: column !important;
      transition: border-color 0.3s ease, box-shadow 0.3s ease !important;
    }

    .thumbnail-strip-{{ section.id }}__card:active,
    .thumbnail-strip-{{ section.id }}__card:hover {
      border-color: #F4C430 !important;
      box-shadow: 0 4px 15px rgba(244, 196, 48, 0.15) !important;
    }
    
    .thumbnail-strip-{{ section.id }}__image-wrapper {
      border-radius: 12px 12px 0 0 !important;
      aspect-ratio: 1 / 1.15 !important;
      overflow: hidden !important;
      width: 100% !important;
      position: relative !important;
    }

    .thumbnail-strip-{{ section.id }}__hover-name {
      position: absolute !important;
      inset: 0 !important;
      display: flex !important;
      align-items: center !important;
      justify-content: center !important;
      background: rgba(0, 0, 0, 0.7) !important;
      color: #FFFFFF !important;
      padding: 20px !important;
      font-size: 16px !important;
      font-weight: 700 !important;
      text-align: center !important;
      opacity: 0 !important;
      transition: opacity 0.3s ease !important;
    }
    
    .thumbnail-strip-{{ section.id }}__card:hover .thumbnail-strip-{{ section.id }}__hover-name {
      opacity: 1 !important;
    }
    
    .thumbnail-strip-{{ section.id }}__info {
      padding: 12px 14px !important;
      background-color: #111111 !important;
      border-radius: 0 0 12px 12px !important;
      display: flex !important;
      flex-direction: row !important;
      justify-content: space-between !important;
      align-items: center !important;
      box-sizing: border-box !important;
    }
    
    .thumbnail-strip-{{ section.id }}__color-wrap {
      display: flex !important;
      align-items: center !important;
      gap: 6px !important;
    }
    
    .thumbnail-strip-{{ section.id }}__dot {
      width: 8px !important;
      height: 8px !important;
      border-radius: 50% !important;
      display: block !important;
      border: 1px solid rgba(255, 255, 255, 0.2) !important;
    }
    
    .thumbnail-strip-{{ section.id }}__text {
      font-size: 11px !important;
      font-weight: 600 !important;
      letter-spacing: 0.5px !important;
      color: #FFFFFF !important;
    }
    
    .thumbnail-strip-{{ section.id }}__price {
      font-size: 11px !important;
      font-weight: 500 !important;
      color: #B0B0B0 !important;
    }
  }
</style>

{% schema %}
{
  "name": "ONE9 Complete Hero",
  "settings": [
    {
      "type": "header",
      "content": "Hero Settings"
    },
    {
      "type": "text",
      "id": "vertical_text",
      "label": "Vertical Brand Text (Comma separated)",
      "default": "O,N,E,9"
    },
    {
      "type": "textarea",
      "id": "title",
      "label": "Heading",
      "default": "INSULATED JACKET\nFOR EVERYDAY WEAR"
    },
    {
      "type": "range",
      "id": "heading_size",
      "label": "Heading Size (Desktop)",
      "min": 20,
      "max": 100,
      "step": 2,
      "unit": "px",
      "default": 56
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subheading",
      "default": "Engineered for warmth. Designed for movement."
    },
    {
      "type": "range",
      "id": "subheading_size",
      "label": "Subheading Size",
      "min": 12,
      "max": 36,
      "step": 1,
      "unit": "px",
      "default": 20
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "SHOP NOW"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Hero Image"
    },
    {
      "type": "select",
      "id": "image_position",
      "label": "Image Position",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "right"
    },
    {
      "type": "header",
      "content": "Marquee Settings"
    },
    {
      "type": "checkbox",
      "id": "show_marquee",
      "label": "Show Marquee",
      "default": true
    },
    {
      "type": "text",
      "id": "marquee_text",
      "label": "Marquee Text",
      "default": "BUILT DIFFERENT. WORN EVERYWHERE."
    },
    {
      "type": "range",
      "id": "marquee_speed",
      "label": "Marquee Speed",
      "min": 10,
      "max": 60,
      "step": 2,
      "default": 30
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Main Background Color",
      "default": "#0A0A0A"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent / Button Color",
      "default": "#F4C430"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Primary Text Color",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "text_secondary_color",
      "label": "Secondary Text Color",
      "default": "#B0B0B0"
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading Color",
      "default": "#F4C430"
    },
    {
      "type": "color",
      "id": "subheading_color",
      "label": "Subheading Color",
      "default": "#B0B0B0"
    },
    {
      "type": "color",
      "id": "marquee_bg_color",
      "label": "Marquee Background",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "marquee_text_color",
      "label": "Marquee Text Color",
      "default": "#8A7B5C"
    },
    {
      "type": "color",
      "id": "card_bg_color",
      "label": "Thumbnail Card Background",
      "default": "#1A1A1A"
    }
  ],
  "blocks": [
    {
      "type": "thumbnail",
      "name": "Thumbnail Card",
      "limit": 4,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Jacket Image"
        },
        {
          "type": "text",
          "id": "product_name",
          "label": "Product Name",
          "default": "Libra Balance Oversized Tee"
        },
        {
          "type": "text",
          "id": "color_name",
          "label": "Color Name",
          "default": "Black"
        },
        {
          "type": "color",
          "id": "color_hex",
          "label": "Color Swatch",
          "default": "#000000"
        },
        {
          "type": "text",
          "id": "price",
          "label": "Price",
          "default": "£129.99"
        },
        {
          "type": "url",
          "id": "product_url",
          "label": "Product Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "ONE9 Complete Hero",
      "category": "Custom",
      "blocks": [
        { "type": "thumbnail", "settings": { "color_name": "Black", "color_hex": "#000000", "price": "£129.99" } },
        { "type": "thumbnail", "settings": { "color_name": "Olive", "color_hex": "#556b2f", "price": "£129.99" } },
        { "type": "thumbnail", "settings": { "color_name": "Brown", "color_hex": "#5c4033", "price": "£129.99" } },
        { "type": "thumbnail", "settings": { "color_name": "Sand", "color_hex": "#c2b280", "price": "£129.99" } }
      ]
    }
  ]
}
{% endschema %}

{% if section.blocks.size > 0 %}
<script>
  document.addEventListener("DOMContentLoaded", function() {
    const heroImage = document.getElementById('heroMainImage');
    if (!heroImage) return;

    const images = [];
    {% for block in section.blocks %}
      {% if block.type == 'thumbnail' and block.settings.image != blank %}
        images.push("{{ block.settings.image | image_url: width: 1200 }}");
      {% endif %}
    {% endfor %}

    if (images.length > 1) {
      let currentIndex = 0;
      setInterval(() => {
        heroImage.style.opacity = '0.7';
        setTimeout(() => {
          currentIndex = (currentIndex + 1) % images.length;
          heroImage.src = images[currentIndex];
          heroImage.removeAttribute('srcset');
          heroImage.style.opacity = '1';
        }, 500); // Wait for fade out
      }, 4000); // Change image every 4 seconds
    }
  });
</script>
{% endif %}
